# Write a shell script to Find a word in a specified location(Path) Using Grep.

#!/bin/bash
echo "Enter Word"
read word
grep -w  $word /home/ubuntu/second.sh > new.txt

if [ -s new.txt ]
then
        echo "Content exit"
else
        echo " Content not exit"
fi


Note:

# -w= word only
